import 'package:flutter/material.dart';

class WorkTripAddScreen extends StatelessWidget {
  const WorkTripAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
