import 'package:flutter/material.dart';

class WorkTripDetailScreen extends StatelessWidget {
  const WorkTripDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
